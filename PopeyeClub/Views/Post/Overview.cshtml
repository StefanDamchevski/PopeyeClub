@model List<PopeyeClub.ViewModels.Post.OverviewViewModel>

@{
    ViewData["Title"] = "Home";
}

@section Styles{
    <link rel="stylesheet" href="~/css/postOverview.css"/>
}

<div class="container">
    @if (Model.Count == 0)
    {
        <div class="not-available">
            <p>No posts to show</p>
        </div>
    }

    @for (int i = 0; i < Model.Count; i++)
    {
        <div class="row">
            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">
                        <div class="post-user">
                            <img src="data:image/jpeg;base64,@Model[i].ProfilePicture" />
                        </div>
                        <a asp-controller="User" asp-action="Profile" asp-route-userId="@Model[i].PostId" class="text-dark hvr-underline-from-center">@Model[i].UserName</a>
                        <div class="post-actions hvr-grow">
                            <i class="fas fa-ellipsis-h fa-2x hvr-icon-grow"></i>
                        </div>
                    </div>
                    <div class="card-body">
                        <img class="post-img" src="data:image/jpeg;base64,@Model[i].PostImage" />
                        <div class="card-actions">
                            @if (Model[i].LikeStatus == PopeyeClub.ViewModels.Like.Enums.PostLikeStatus.None)
                            {
                                <button class="btn hvr-pulse-grow" onclick="addLike(event,@Model[i].PostId)">
                                    <i class="far fa-heart fa-2x hvr-icon-pulse-grow"></i>
                                </button>
                                <button class="btn hvr-pulse-grow hide" onclick="removeLike(event, @Model[i].PostId)">
                                    <i class="fas fa-heart fa-2x hvr-icon-pulse-grow"></i>
                                </button>
                            }
                            else
                            {
                                <button class="btn hvr-pulse-grow hide" onclick="addLike(event,@Model[i].PostId)">
                                    <i class="far fa-heart fa-2x hvr-icon-pulse-grow"></i>
                                </button>
                                <button class="btn hvr-pulse-grow" onclick="removeLike(event, @Model[i].PostId)">
                                    <i class="fas fa-heart fa-2x hvr-icon-pulse-grow"></i>
                                </button>
                            }
                            <a class="hvr-pulse-grow">
                                <i class="far fa-comment-alt fa-2x hvr-icon-pulse-grow"></i>
                            </a>
                            <a class="hvr-pulse-grow">
                                <i class="far fa-share-square fa-2x hvr-icon-pulse-grow"></i>
                            </a>
                            <a class="hvr-pulse-grow">
                                <i class="far fa-bookmark fa-2x hvr-icon-pulse-grow"></i>
                            </a>
                        </div>
                        <div class="likes-count-section">
                            <span>Liked by <span>@Model[i].PostLikes.Where(x => x.Status.Equals(true)).Count()</span> people</span>
                        </div>
                        <div class="comments-section" id="commentSection-@i">
                            <div class="view-comment-button">
                                <a class="text-primary hvr-underline-from-center">View All <span>@Model[i].PostCommentsCount</span> Comments</a>
                            </div>
                            @if (Model[i].PostComments.Count == 0)
                            {
                                <div class="not-available">
                                    <p>No comments on this post</p>
                                </div>
                            }
                            @for (int y = 0; y < Model[i].PostComments.Count; y++)
                            {
                                <div class="comment">
                                    <div class="user-comment">
                                        <img src="data:image/jpeg;base64,@Model[i].PostComments[y].UserImage" />
                                        <span>@Model[i].PostComments[y].UserName</span>
                                    </div>
                                    <div class="comment-text">@Model[i].PostComments[y].Text</div>
                                    <div class="comment-like">
                                        @if (Model[i].PostComments[y].CommentLikeStatus == PopeyeClub.ViewModels.Like.Enums.CommentLikeStatus.None)
                                        {
                                            <button class="btn hvr-grow" onclick="addCommentLike(event,@Model[i].PostComments[y].Id)">
                                                <i class="far fa-thumbs-up fa-2x hvr-icon-grow"></i>
                                            </button>
                                            <button class="btn hvr-grow hide" onclick="removeCommentLike(event,@Model[i].PostComments[y].Id)">
                                                <i class="fas fa-thumbs-up fa-2x hvr-icon-grow"></i>
                                            </button>
                                        }
                                        else
                                        {
                                            <button class="btn hvr-grow hide" onclick="addCommentLike(event,@Model[i].PostComments[y].Id)">
                                                <i class="far fa-thumbs-up fa-2x hvr-icon-grow"></i>
                                            </button>
                                            <button class="btn hvr-grow" onclick="removeCommentLike(event,@Model[i].PostComments[y].Id)">
                                                <i class="fas fa-thumbs-up fa-2x hvr-icon-grow"></i>
                                            </button>
                                        }
                                    </div>
                                </div>
                                <hr />
                            }
                        </div>
                        <div class="created-days-ago">
                            <p>@Model[i].DaysAgo Days Ago</p>
                        </div>
                        <div class="card-footer">
                            <form  class="form-inline" method="post" onsubmit="addComment(event, @i)">
                                <input type="hidden" name="postId" value="@Model[i].PostId" />
                                <div class="form-group comment-form">
                                    <input type="text" name="comment" class="comment-input" id="commentInput-@i" onkeyup="enablePostBtn(event)" placeholder="Write a comment..." required/>
                                </div>
                                <div class="form-group">
                                    <input type="submit" value="Post" class="comment-submit btn btn-outline-primary" />
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
@section Scripts{
    <script src="~/lib/axios/axios.min.js"></script> 
    <script src="~/js/postOverview.js"></script>
}